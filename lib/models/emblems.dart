class Emblem {
  final String title, image, quiz, color;
  final int minPoints, maxPoints;

  Emblem({
    required this.title,
    required this.color,
    required this.image,
    required this.maxPoints,
    required this.minPoints,
    required this.quiz,
  });
}
